/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove any appDir-related configuration
  reactStrictMode: true,
  // Add other configurations as needed
}

module.exports = nextConfig